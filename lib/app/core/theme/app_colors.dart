import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFFFF9800); // laranja DB
  static const secondary = Color(0xFF1A237E); // azul escuro
  static const background = Color(0xFF0E0E0E);
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFBDBDBD);
}
